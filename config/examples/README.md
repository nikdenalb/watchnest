# Configuration Examples

Use this directory for safe configuration templates.

Example files may define required variable names, defaults that are safe to
publish, and short comments. They must not contain real secrets or private
environment-specific values.

Add module-specific examples only after the corresponding module is introduced.
