# Example local overrides for planner-app.
# Copy to an ignored file such as .env.planner-app and load it in your shell or IDE run config.

SERVER_PORT=8080
WATCHNEST_FRONTEND_ORIGIN=http://localhost:5173
WATCHNEST_SESSION_COOKIE_SECURE=false
